---
title: Colors
description: Sample Colors documentation
layout: ../layouts/MainLayout.astro
---

import {
  ColorWrapper,
  ColorsWrapper,
  colorsInfo,
} from '@selleo/core/src/Colors';
import { H2 } from '@selleo/core/src/Headers';
import Preview from '../components/Preview.astro';
import Description from '../components/Description.astro';

<Description>
  Below you can find our brand colors styled with tailwindcss.
</Description>

<H2>Brand Colors</H2>

<div class="preview">
{colorsInfo.brand.primary.sectionOne.map(({ class: classes, colorInfo, size }) => <ColorWrapper colorInfo={colorInfo} size={size} class={classes} />)}

<ColorsWrapper itemsLength={4} class="mb-7 mt-3">
  {colorsInfo.brand.primary.sectionTwo.map(
    ({ class: classes, colorInfo, size }) => (
      <ColorWrapper colorInfo={colorInfo} size={size} class={classes} />
    )
  )}
</ColorsWrapper>

{colorsInfo.brand.secondary.sectionOne.map(({ class: classes, colorInfo, size }) => <ColorWrapper colorInfo={colorInfo} size={size} class={classes} /> )}

<ColorsWrapper itemsLength={3} class="mb-7 mt-3">
  {colorsInfo.brand.secondary.sectionTwo.map(
    ({ class: classes, colorInfo, size }) => (
      <ColorWrapper colorInfo={colorInfo} size={size} class={classes} />
    )
  )}
</ColorsWrapper>

<H2>Neutral Colors</H2>

{colorsInfo.neutral.map(({ class: classes, colorInfo, size }) => <ColorWrapper colorInfo={colorInfo} size={size} class={classes} /> )}

<H2>States Colors</H2>

{colorsInfo.states.map(({ class: classes, colorInfo, size }) => <ColorWrapper colorInfo={colorInfo} size={size} class={classes} /> )}

</div>
